# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Env / secrets
.env
.env.*
!.env.example

# Docker / local state
qdrant_storage/
langfuse_data/
qdrant_local_data/
eval/local_qdrant_data/

# OS / editor
.DS_Store
.vscode/
.idea/

# Logs
*.log

# Excel lock files
~$*.xlsx

# DeepEval local run cache
.deepeval/

# Local experimentation UI's own scratch output (not eval ground truth)
dev_ui/relevance_feedback.jsonl

# Claude Code local session/runtime state (launch.json is the only shareable part)
.claude/settings.local.json
.claude/scheduled_tasks.lock
